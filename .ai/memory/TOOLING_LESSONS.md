# Memory — Tooling Lessons

> **Integration and tooling lessons with future value**: quirks, gotchas, and setups worth remembering across projects. Version-sensitive lessons should note the version and may point to `../knowledge/`. Add only per the update rules in `README.md`. **No secrets, tokens, or keys — ever.**

## Entries

| Date | Tool / integration | Lesson (future value) | Versions | Related knowledge |
|------|--------------------|-----------------------|----------|-------------------|
| _(none yet)_ | — | — | — | — |

## How to add

Add an integration/tooling lesson only when it has **clear future value** (would save real time or prevent a repeat problem). Note affected versions — tooling lessons stale; re-verify before relying on an old one. Never record credentials; reference the secret store instead.
