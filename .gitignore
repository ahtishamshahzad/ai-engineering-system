# OS / editor
.DS_Store
*.swp
.vscode/
.idea/

# Agent worktrees (not part of the system)
.claude/worktrees/

# Node / deps (none in Phase 1; guard for later phases)
node_modules/
*.log

# Secrets — never commit real env files
.env
.env.local
.env*.local
